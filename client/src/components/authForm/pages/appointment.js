import React from 'react';

const Appointments = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Appointments lists</h1>
    </div>
  );
};

export default Appointments;
